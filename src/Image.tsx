/**
 * This renders a <img> tag for the current item, using a number of styles that
 * make sense.
 *
 * // Ensures that if the image is larger than the width, we do not exceed the proportions
 * .img {maxWidth: '100%'}
 *
 * cursor: pointer; // go to next
 *
 * max-height: calc(100vh - 140px);
 */

export const Image = 1;